🟡 10 задач среднего уровня

Работа с файлами, конфигами и многосерверными операциями.

Скопировать файл с локальной машины на сервер с помощью SFTPClient.put().

Скопировать лог с сервера на локальную машину с помощью SFTPClient.get().

Проверить, существует ли файл /etc/hosts на удалённой машине (через выполнение test -f /etc/hosts && echo OK).

Выполнить команду systemctl status nginx и определить, запущен ли сервис.

Написать функцию, которая принимает список команд и выполняет их по очереди на сервере.

Выполнить команду cat /etc/os-release и вывести название дистрибутива.

Реализовать подключение с использованием ключа SSH (без пароля).

Написать скрипт, который читает список серверов из servers.txt и выполняет команду uptime на каждом.

Реализовать параллельное выполнение команд на нескольких серверах (через threading).

Добавить логирование в файл всех результатов команд (используя logging).
