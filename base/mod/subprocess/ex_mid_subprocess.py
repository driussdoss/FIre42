⚙️ 10 СРЕДНИХ ЗАДАЧ

💡 Работа с выводом, пайпами, логами, переменными окружения.

Прочитать stdout и stderr отдельно
Выполни команду, которая выдаёт ошибку (например, ls nonexistent), и выведи обе строки.

Передать переменные окружения
Используй env={"MODE": "dev"} при вызове subprocess.run().

Запуск нескольких команд подряд
Выполни mkdir test && ls через Python.

Использовать subprocess.Popen
Выполни ping -c 2 google.com, выведи результат построчно во время выполнения.

Собрать вывод двух команд (пайп)
Пример: ps aux | grep python через subprocess.Popen().

Перенаправить вывод в лог
Выполни любую команду и сохрани stdout в файл log.txt.

Обработка ошибок
Попробуй запустить несуществующую команду, перехвати исключение, выведи "Ошибка!".

CLI-утилита через Python
Реализуй Python-скрипт, который выполняет любую команду, переданную как аргументы (sys.argv[1:]), и печатает вывод.

Ограничение по времени
Выполни sleep 5 с timeout=2 и обработай subprocess.TimeoutExpired.

Запуск команды в фоне
Выполни ping -c 3 google.com через Popen, не ожидая завершения (без .wait()).
