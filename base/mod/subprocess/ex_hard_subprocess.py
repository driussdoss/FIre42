🚀 10 СЛОЖНЫХ ЗАДАЧ (DevOps-практика)

💡 Практика близка к реальности: работа с логами, пайплайнами, SSH, health-check, деплоем и сбором информации о системе.

Health-check сервера
Запусти ping -c 1 8.8.8.8.
Если код возврата 0 — выведи "Server reachable", иначе "Server down".

Мониторинг ресурса
Выполни df -h и выведи строку, где упомянуто /.

Проверка статуса git
Выполни git status, получи текст и определи, есть ли "nothing to commit".

Автоматический деплой (симуляция)
Скрипт должен:

Выполнить git pull

Затем systemctl restart nginx

Вывести результат обеих команд

Сбор системной информации
Выполни uname -a, df -h, uptime и выведи всё в один файл sysinfo.txt.

Фоновый процесс + логирование
Запусти ping google.com в фоне через Popen, сохрани stdout в ping.log.

SSH через subprocess
Выполни команду:

ssh user@server "uptime"


(для теста можно просто замокать вывод через echo).

Backup-скрипт
Создай архив через tar -czf backup.tar.gz /etc
Если код возврата 0 — выведи "Backup complete".

Проверка зависимостей
Пройди по списку ["git", "python3", "docker"] и проверь, установлены ли они (which name).

Реализация мини-shell
Реализуй цикл, принимающий ввод пользователя (input("> ")) и выполняющий команду через subprocess.run()
(до тех пор, пока не введено exit).
