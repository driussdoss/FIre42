🧩 10 ПРОСТЫХ ЗАДАЧ

💡 Цель — понять базовые принципы: как запускать команды, получать вывод и статус.

Вывести дату
Используй subprocess.run(["date"]) — выведи текущую дату и время.

Вывести текущую директорию
Команда: ls или dir (в зависимости от ОС).

Получить результат команды
Используй subprocess.run(["echo", "Hello"], capture_output=True, text=True) и выведи .stdout.

Проверить код возврата
Выполни любую команду и выведи .returncode.

Запуск с ошибкой
Выполни несуществующую команду и обработай subprocess.CalledProcessError.

Вывести список файлов
Выполни ls -l и выведи результат в консоль.

Передать аргументы в команду
Выполни echo "Your name" с переменной из Python.

Перенаправление вывода в файл
Сохрани результат команды ls в output.txt.

Проверить наличие команды
Используй subprocess.run(["which", "python3"]) — выведи путь.

Запустить ping
Выполни ping -c 1 google.com и выведи "OK", если код возврата 0.
