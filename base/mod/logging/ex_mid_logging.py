🟡 10 СРЕДНИХ ЗАДАЧ (структурирование, хэндлеры, форматирование)

Цель — освоить работу с несколькими хэндлерами и раздельными логами.

Два логера:
Создай два логгера: app_logger и db_logger.
Один пишет в консоль, другой — в файл.

Разные уровни для хэндлеров:
console → INFO
file → DEBUG
Проверь, что отладка пишется только в файл.

Свой формат:
Настрой формат:
2025-10-08 12:00:00 | INFO | main.py:42 | Сообщение

Разделение логов по типам:
Ошибки (ERROR+) отправляй в errors.log,
всё остальное — в app.log.

Перенаправление stdout:
Настрой logging так, чтобы print() и logging.info() шли в один лог.

Уровень из переменной окружения:
Пусть уровень логирования (DEBUG, INFO, ...) задаётся через os.environ["LOG_LEVEL"].

Отдельная функция логирования:
Напиши функцию get_logger(name) — создаёт логер с готовым форматом и хэндлером.

Логирование при работе с файлами:
Открой файл, прочитай содержимое, при ошибке чтения выведи logging.error().

Измерение времени:
Логируй время начала и конца выполнения кода, вычисли длительность операции.

Переиспользуемый конфиг:
Вынеси конфигурацию логирования в отдельный модуль logger_config.py и импортируй.
