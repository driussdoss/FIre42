🟡 Средние

Попробуй подключиться к несуществующему файлу — отлови FileNotFoundError.

Используй logging для записи всех ошибок в файл.

Напиши скрипт, который вызывает внешнюю команду (ls /root) и отлавливает subprocess.CalledProcessError.

Добавь повтор выполнения операции (3 раза при ошибке).

В одном try блоке обрабатывай 2 типа ошибок (OSError, ValueError).
