🔵 Сложные (DevOps уровень)

Оберни вызов AWS API (boto3) в try/except, чтобы логировать ошибки доступа.

Реализуй “автовосстановление” EC2-инстанса при ClientError.

Напиши скрипт, который читает конфиг и при ошибке KeyError создаёт дефолтный.

Сделай логирование с уровнем CRITICAL, если SSH неудачно подключается 3 раза подряд.

Скомбинируй try/except + finally для безопасного деплоя (закрытие SSH).

Используй raise внутри except, чтобы пробросить ошибку дальше.

Реализуй “graceful shutdown” — обрабатывай KeyboardInterrupt при остановке скрипта.

При ошибке соединения с API записывай время и ошибку в JSON-лог.

Напиши класс SafeRunner, который выполняет команды и логирует любые исключения.

Имитация CI: запускаешь несколько задач, каждая обёрнута в try/except, ошибки не ломают пайплайн.
