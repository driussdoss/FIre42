🔴 10 сложных задач (приближено к DevOps-реальности)

Эти задачи уже практические и близки к автоматизации в инфраструктуре.

Написать скрипт, который подключается к группе серверов и проверяет свободное место (df -h), уведомляя если <10%.

Реализовать деплой приложения: загрузить архив .tar.gz, распаковать его на сервере, перезапустить сервис.

Создать резервную копию /etc каталога на удалённой машине и скачать архив на локальную.

Выполнить серию команд для обновления системы (apt update && apt upgrade -y) и сохранить лог.

Реализовать команду удаления старых логов: find /var/log -type f -mtime +7 -delete и записать результат.

Реализовать менеджер конфигураций: отправить шаблон конфигурационного файла на несколько серверов.

Проверить доступность сервера по SSH (timeout 5 секунд) перед выполнением операций.

Подключиться к группе серверов и собрать версии nginx, docker, python3 в один CSV.

Реализовать функцию перезапуска сервиса (systemctl restart service_name) и проверку статуса после.

Реализовать удалённое выполнение команд и загрузку файлов по ключевой аутентификации, добавив CLI-интерфейс (argparse).
