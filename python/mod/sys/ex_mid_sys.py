⚙️ 10 СРЕДНИХ ЗАДАЧ

💡 Работа с аргументами, вводом/выводом, кодом выхода — как у CLI-утилит.

Принять аргумент и вывести его
Скрипт принимает одно слово через sys.argv[1] и выводит его в консоль.

Проверка флагов
Если запущено с аргументом --debug, выведи "Режим отладки включен".

Простая CLI-утилита
Аргументы:

--sum 2 3 → вывести 5

--mul 2 3 → вывести 6

Безопасное завершение
Если не переданы аргументы — завершай через sys.exit("Нет аргументов").

Вывод с перенаправлением
Направь текст "Ошибка!" в sys.stderr.write().

Проверка Python-версии
Если версия меньше 3.8 — завершай с сообщением "Нужен Python 3.8+".

Счётчик аргументов
Выведи, сколько аргументов передано (не считая имени скрипта).

CLI для приветствия
При запуске: python greet.py user → выведи "Привет, user!".

Вывод переменной окружения PYTHONPATH
Выведи путь, если он присутствует в sys.path.

Интерактивный ввод
Используй sys.stdin.readline() — прочитай строку и выведи её обратно.
