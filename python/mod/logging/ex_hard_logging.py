🔴 10 СЛОЖНЫХ ЗАДАЧ (реальные DevOps сценарии)

Цель — применять logging в боевых условиях: деплой, мониторинг, CI/CD, cron, и работа с subprocess.

Health-check логирование:
Каждую минуту выполняй ping google.com
→ если ответ успешный — INFO,
→ если нет — WARNING.

Мониторинг ресурсов:
Логируй результат df -h через subprocess,
если диск заполнен >80%, пиши ERROR.

Лог деплоя:
В deploy.log записывай:

начало деплоя,

успешное завершение,

ошибку при subprocess.run(["git", "pull"]).

Cron-скрипт с логами:
Создай скрипт, который каждые 10 секунд логирует "Сервис активен",
а при ошибке — logging.exception().

Отдельные логи для stdout и stderr:
Направь stdout в info.log, а stderr — в error.log.

Ротация логов:
Используй logging.handlers.RotatingFileHandler,
чтобы старые логи сохранялись как app.log.1, app.log.2, и т.д.

Логирование SSH-запросов:
Выполни ssh user@host uptime через subprocess и логируй результат (или ошибку соединения).

Цветной вывод логов в консоль:
Реализуй кастомный форматтер, который добавляет цвет для уровней:
INFO — зелёный, WARNING — жёлтый, ERROR — красный.

Web API logger:
Напиши логгер, который пишет запросы вида:
INFO: GET /api/users 200 OK (120ms)
с временем выполнения.

CI/CD отчёт:
Запусти 3 команды (build, test, deploy) через subprocess,
все результаты логируй в pipeline.log с временем и статусом (успешно / ошибка).
